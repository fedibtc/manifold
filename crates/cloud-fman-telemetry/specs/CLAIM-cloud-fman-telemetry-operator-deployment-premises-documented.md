# CLAIM-cloud-fman-telemetry-operator-deployment-premises-documented: Deployment material identifies external operator premises

The shipped collector deployment material identifies, without claiming
fulfillment, one active container on one commonly mounted encrypted persistent
volume; exact ownership and mode provisioning; read-only key delivery and
separate runtime, provisioning, and backup custody; real private-listener
network isolation; deployed source correspondence; external Prometheus
timestamp/staleness behavior and Grafana/backend ownership; stopped and coupled
SQLite/WAL/archive/key backup and restore; capacity and destructive-storage
qualification; readiness before traffic; and 75-second termination grace as
operator or external-system premises.

## Status

Unverified.

## Assumptions

- A statement that an operator or external system must supply a control is
  distinct from evidence that the operator or system actually supplies it.
