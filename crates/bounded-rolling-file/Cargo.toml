[package]
name = "bounded-rolling-file"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
rustix = { version = "1", features = ["fs"] }
uuid = { version = "1", features = ["v7"] }

[dev-dependencies]
tempfile = "3"
