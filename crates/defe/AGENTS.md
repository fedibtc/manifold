# defe notes

- Read [`./SECURITY.md`](./SECURITY.md) before changing process spawning, socket handling, temp paths, or resource descriptors.
- Keep implementation-facing documentation in [`./docs/`](./docs/) synchronized with API, CLI, resource, and testing behavior.
