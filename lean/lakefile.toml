defaultTargets = ["FMan"]
name = "fman"

# Deliberately dependency-free: the models use only Lean core, so `lake build`
# is offline and the Nix check needs no fetched packages.

[[lean_lib]]
name = "FMan"
